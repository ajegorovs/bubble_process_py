pip install opencv-contrib-python
pip install more-itertools
pip install networkx[default]
pip install alphashape
pip install glob2
pip install -U scikit-image
pip install descartes
pip install tqdm
pip install scikit-image    #not used anymore